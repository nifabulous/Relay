# PR #7 Review Fixes Implementation Plan

> **For agentic workers:** Execute this plan task-by-task with tests first.

**Goal:** Correct unsafe tracking behavior, make the expanded seed data roll forward on populated databases, expose SSI load failures in Explore, repair CI lint, and refresh project counts.

**Architecture:** Keep the prepare endpoint as a recommendation-only operation and only create simulated timelines for recommendations that are safe to proceed. Make the static seed loader idempotent by upserting current rows and applying the known BIC corrections. Treat bank lookup and SSI loading as separate async regions in the directory UI.

**Tech Stack:** FastAPI, SQLAlchemy, pytest, React, TanStack Query, Vitest, MSW, Ruff.

## Global Constraints

- Do not overwrite unrelated user changes.
- Preserve the existing tracking API contract for explicitly created simulations.
- Add regression tests before production changes for behavior fixes.
- Keep all seed account numbers synthetic placeholders.

### Task 1: Tracking safety

**Files:**
- Modify: `tests/test_prepare.py`
- Modify: `app/services/prepare.py`
- Modify: `frontend/src/features/operate/prepare/PreparePaymentPage.tsx`
- Modify: `frontend/src/features/operate/prepare/PreparePaymentPage.test.tsx`

- [x] Add failing backend tests proving blocking and review recommendations do not create trackable credited timelines.
- [x] Add failing frontend tests proving the tracking link is hidden for blocking and review results.
- [x] Gate timeline persistence and the link to explicitly trackable recommendations.

### Task 2: Seed rollout

**Files:**
- Modify: `tests/test_data_consistency.py`
- Modify: `app/services/seed.py`

- [x] Add a failing populated-database test for new rows and corrected BICs.
- [x] Implement idempotent missing-row inserts plus the known old-to-new BIC corrections without overwriting imported SSI values.
- [x] Verify fresh and populated databases remain duplicate-free.

### Task 3: Explore SSI errors

**Files:**
- Modify: `frontend/src/features/explore/ExplorePage.test.tsx`
- Modify: `frontend/src/features/explore/ExplorePage.tsx`

- [x] Add a failing MSW test for SSI error, visible retry, and successful retry.
- [x] Render SSI loading/error/empty states without masking a valid bank lookup.

### Task 4: CI and documentation

**Files:**
- Modify: `app/services/prepare.py`
- Modify: `README.md`
- Modify: `CLAUDE.md`

- [x] Fix the Ruff import ordering.
- [x] Refresh the documented data and test counts after verification.

### Task 5: Verification

- [x] Run focused backend and frontend regression tests.
- [x] Run the complete backend and frontend suites.
- [x] Run Ruff, frontend typecheck/build, bundle budget, and targeted E2E checks.
